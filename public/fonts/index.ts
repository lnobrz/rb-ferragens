import { Tomorrow, Kanit } from "@next/font/google";

export const tomorrow = Tomorrow({
  weight: ["400", "500"],
  subsets: ["latin"],
});

export const kanit = Kanit({
  weight: ["300", "400"],
  subsets: ["latin"],
});
